# Synthetic variance report toy repository

This disposable fixture exists only to exercise the repository-owned local MCP server. It contains invented code and data, makes no network calls, and is copied to a temporary directory before any write-capable tool is enabled.
